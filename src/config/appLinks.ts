export const appLinks = {
  // Add the approved channel URL here before release.
  houseArauzUrl: null as string | null,
  // Add the approved support address here before release.
  supportEmail: null as string | null,
} as const;
