// Keep approved local alerts active until the Phase 11D backend is deployed and verified.
// Change to true only in the separately approved deployment/integration step.
export const remoteWitchWatchEnabled = false;
