import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function WaitTimesScreen() {
  return (
    <PlaceholderScreen
      title="Wait Times"
      description="Attraction wait times will appear here when the live data experience is ready."
      icon="people-outline"
    />
  );
}
